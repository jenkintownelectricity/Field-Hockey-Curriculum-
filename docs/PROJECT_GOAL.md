# Project Goal

Build a kernel-first field hockey coaching SaaS (PWA + iOS/Android via Capacitor)
that can run a complete program from home, generate PDFs, SVG diagrams,
YouTube scripts, storyboards, and pocket cards.
